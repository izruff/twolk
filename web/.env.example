# Vite only exposes vars prefixed with VITE_ to the client.

# Base URL of the REST API used by the frontend.
VITE_REST_API_BASE_URL=http://localhost:8000/v1

# URL of the WebRTC signaling backend (HTTPS, self-signed cert in dev).
VITE_SIGNALING_SERVER_URL=https://localhost:3000
